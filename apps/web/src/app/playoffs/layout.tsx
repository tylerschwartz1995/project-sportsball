import "@/features/playoffs/styles.css";
import "@/features/playoffs/presentation.css";
import type { ReactNode } from "react";
export default function Layout({ children }: { children: ReactNode }) { return children; }
